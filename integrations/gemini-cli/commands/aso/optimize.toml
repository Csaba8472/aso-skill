description = "Quick metadata optimization: generate copy-paste ready titles and descriptions"
prompt = """
Use the `app-store-optimization` skill (and specialized subagent `aso-optimizer`) to optimize app metadata.

**Workflow:**
1. Gather app details and target keywords.
2. Generate Apple/Google metadata fitting character limits.
3. Validate all outputs are copy-paste ready.
4. Output to `outputs/[app-name]/02-metadata/`.
"""
